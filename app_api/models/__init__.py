"""Pydantic models package.

This package defines the data schemas used by the FastAPI application.

The models ensure validation and serialization of data exchanged between:
- the API endpoints
- the database layer
- the frontend client

They define the structure of the data accepted and returned by the API.
"""
